name: academic_web
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1 # Para fazer chamadas a API futuramente
  intl: ^0.19.0 # Para formatar datas

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  bdd_widget_test: ^2.1.3 # Versão corrigida para compatibilidade

flutter:
  uses-material-design: true